# npm-cli configuration read by `npm version` (which `pnpm version` proxies to): release tags and version commits are namespaced per package in this monorepo. pnpm itself reads no settings from .npmrc — those live in pnpm-workspace.yaml — so this file only shapes the version flow.
tag-version-prefix=tailwind-merge@
message=tailwind-merge@%s
